
Requires:

- Java
- Groovy
- Apache POI
    - set POI_HOME to the distribution

Usage:

- exec setvars.sh or equivalent, to set the classpath
- "groovy Convert.groovy file" will write HTML to stdout
